APP_PASSWORD=replace-with-a-long-random-password
# RunPod's HTTPS proxy requires secure cookies. Set false only for local HTTP development.
COOKIE_SECURE=true
# Optional: default is 20 GiB; this also limits a project ZIP's extracted files.
MAX_UPLOAD_GB=20
# Optional: default is 8 MiB. Use a smaller whole-MiB value for slower or less reliable proxies.
UPLOAD_CHUNK_MB=8
# Shared RunPod network-volume path. Override to a local folder for non-container development.
WORKSPACE_ROOT=/workspace/blendrender

# Leave unset for local Blender development. Production and E2E images enable the bundled
# FLIP Fluids Demo add-on automatically.
# FLIP_FLUIDS_ADDON=
# FLIP_FLUIDS_BOOTSTRAP_SCRIPT=

# Optional: required only by the RunPod S3 scene preparation and cleanup scripts. Obtain these from
# RunPod's S3 API Keys settings, and never commit real credential values.
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# RUNPOD_NETWORK_VOLUME_ID=
# RUNPOD_S3_REGION=EUR-IS-1
# Optional override; defaults to the lowercased-region RunPod S3 endpoint.
# RUNPOD_S3_ENDPOINT=
